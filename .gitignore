# Gradle files
.gradle/
build

# Coverage badge scratch dir (badge JSON lives on the `badges` branch, not main)
coverage-output/

# Local configuration file (sdk path, etc)
local.properties

# Log/OS Files
*.log
.DS_Store

# Android Studio generated files and folders
captures/
.externalNativeBuild/
.cxx/
*.aab
*.apk
output-metadata.json

# IntelliJ
*.iml
.idea/
misc.xml
deploymentTargetDropDown.xml
render.experimental.xml

# Claude Code local settings
.claude/settings.local.json

.omc

# Keystore files
*.jks
*.keystore

# Google Services (e.g. APIs or Firebase)
google-services.json

# Android Profiling
*.hprof

# Node.js (commit analyzer, release-notes generator, commitlint)
node_modules/

# Scratch files written by the release and commit-lint pipelines. They are
# per-run artifacts of scripts/release.sh and the workflows that call the
# preview scripts; nothing here belongs in a commit.
/analyzer.log
/comment.json
/comment.md
/commitlint.log
/notes.md
/preview.json
/signoff-comment.json
/signoff-comment.md
